purpose of miniproject A is to just get familiar with the steps of what makes a blockchain.
following the guides provided on D2L and some of the utility code was helpful to understand what
we are doing with the data and the hashes in a block and how we add the simple blocks to the chain.

This is a simple blockchain next step would be to add the actual data we want which is firstname, lastname, DOB, or
UUID for example. As well as using JSON and JSON files to fill our data for the block.